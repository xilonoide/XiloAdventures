<Window x:Class="XiloAdventures.Wpf.Windows.DockerProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Preparando servicios de IA"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        ShowInTaskbar="False"
        Background="#1E1E1E"
        Foreground="#F5F5F5">
    <Border Margin="16"
            CornerRadius="8"
            Background="#2A2A2A"
            BorderBrush="#444444"
            BorderThickness="1">
        <StackPanel Margin="4">
            <TextBlock x:Name="MessageText"
                       Text="Docker esta haciendo sus cosas..."
                       Margin="4,0,4,8"
                       TextWrapping="Wrap" />
            <ProgressBar IsIndeterminate="True"
                         Height="18"
                         Margin="4,0,4,8" />
            <TextBlock x:Name="DetailText"
                       Margin="4,0,4,0"
                       TextWrapping="Wrap"
                       FontSize="11"
                       Opacity="0.8" />
        </StackPanel>
    </Border>
</Window>
